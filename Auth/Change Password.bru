meta {
  name: Change Password
  type: http
  seq: 5
}

post {
  url: {{BASE_URL}}/auth/change-password
  body: json
  auth: inherit
}

body:json {
  {
    "password": "password",
    "new_password": "password"
  }
}
